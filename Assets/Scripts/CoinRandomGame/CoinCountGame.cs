using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CoinCountGame : MonoBehaviour
{
    /*public Text coinCountText;
    
    // Start is called before the first frame update
    void Start()
    {
        ShuffleManager OnClicked = gameObject.GetComponent<ShuffleManager>();
        OnClicked = OnClicked.gameObject.GetComponent<ShuffleManager>().OnClicked();
    }


    public void IncreaseCoin()
    {
        if ( OnClicked.GetName() == "Coin100")
        {
            coinCountText = coinCountText + 100;
        }
    }*/
}
